{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Setup venv & install deps",
            "type": "shell",
            "command": "test -d .venv || python3 -m venv .venv && .venv/bin/pip install -e '.[dev]'",
            "group": "build",
            "presentation": {
                "reveal": "silent",
                "panel": "shared"
            }
        }
    ]
}
